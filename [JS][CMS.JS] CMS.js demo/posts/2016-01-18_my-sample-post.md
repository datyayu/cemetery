---
layout: post
title: Example post
---

## Hello world
Ok so if this show on the first try, then cmsjs rocks.
If don't, then I probably fucked something up.


### JS code test.
```
    (function() {
        var hello = 'hello';
        let number = 1;
        const array = [1, 2, 3, 4];
        
        array
            .map(number => number + 1)
            .forEach((number) => {
                console.log(number);
            });
    })();
```